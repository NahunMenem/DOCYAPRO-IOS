import 'package:flutter/material.dart';

class ConsultasEnCursoScreen extends StatelessWidget {
  const ConsultasEnCursoScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("Consulta en curso"));
  }
}
