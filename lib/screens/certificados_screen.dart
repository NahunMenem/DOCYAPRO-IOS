import 'package:flutter/material.dart';

class CertificadosScreen extends StatelessWidget {
  const CertificadosScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("📄 Emitir Certificados"),
    );
  }
}
